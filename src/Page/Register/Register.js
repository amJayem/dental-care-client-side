import React from 'react';
import SetTitle from '../../hooks/setTitle';

const Register = () => {
    SetTitle(Register);
    return (
        <div>
            Register
        </div>
    );
};

export default Register;